# 开发记录与待办

一份给自己看的清单：现在有什么、为什么这么做、接下来能做什么。
数值类的结论都来自 `npm run balance` / `npm run check`，不是拍脑袋。

## 一、现状速览

- 纯静态 Web 游戏，零依赖零构建（ES module + Canvas 2D），`npm run serve` 起本地服务，GitHub Pages 部署。
- 237 个测试（`npm test`）、13 条平衡断言（`npm run check`）、内容表校验（`npm run validate`）、
  架构约束（`npm run arch`）、体积检查（`npm run size`）、确定性自检（`npm run selftest`）都在 CI 里。
  本地调数值用 `npm run check:quick`（约 20 秒，全量要 105 秒）。
- 一局的完整链路：首屏选角色/难度 → 走四个区域 → 打倒最后一个区域的 Boss 通关 → 继续无尽或重开 → 结算残片 → 局外解锁。
- 内容规模：7 基础 + 7 进化 + 3 觉醒武器、4 技能、9 词条 + 3 诅咒、6 战利品、8 兵种、
  3 精英原型、4 Boss 原型、4 区域、4 角色、2 难度、3 永久强化、12 成就。
- 代码规模：34 个模块 / 约 8.5K 行（`src/`），gzip 后 135KB（上限 220KB）。
  最大的几个：`world-render.js` 883、`game.js` 758、`weapons.js` 744、`sim.js` 709、`panels.js` 657。

## 二、下次从这里接上（2026-09-10 收尾）

这一节是给"没有上下文的下一次"写的。前面几节讲的是"有什么"，这一节讲**站在哪儿、下一步动哪只脚**。

### 最后做完的三件事（都在表现层，逻辑一个字节没动）

1. **叠加特效层**：光相关的绘制（拖尾 / 粒子 / 闪电 / 光环 / 光晕）走 `'lighter'`，实体走默认模式。
   入口只有 `world-render.js` 的 `additive(fn)`，有断言守着"进出配平 + 最后一次是归位"。
2. **光晕精灵**（`view/glow.js`）：一种颜色一张 64×64 离屏图，之后每帧只 `drawImage`。
   用在玩家柔光、升级闪光、Boss/精英底光、环绕球、大经验球、进化与觉醒弹体。
3. **背景三层 + 区域辨识**：远景斑点（0.45 倍视差）→ 区域各自的地面图案（方格/波纹/蜂窝/十字点）→ 地形；
   区域色调 0.07 → 0.11。图案登记表是 `shapes.js` 的 `GROUND_PATTERNS`，`validate.js` 核对。

顺带把 124 处 `ctx.font` 收口进 `view/font.js`（canvas 不继承 CSS 字体栈）。

### 回来后的第一件事：试玩，不是写代码

上面这些**没有任何一条被人眼验证过**（开发环境看不到画面，只有测试和断言）。
所以下次的第一步是跑两三局，回答这几个问题，再决定要不要继续磨表现层：

- 光晕会不会太抢眼？相关常量：玩家柔光 `world-render.js` 里 `w.player.r * 3.4` / alpha `0.42`；
  Boss 底光 `e.r * 2.8` / `0.5`；弹体 `b.r * 3.6` / `0.55`；大经验球 `g.r * 3.4` / `0.5`。
- 叠加后粒子密集处会不会过曝？粒子透明度量化 8 档在 `PARTICLE_ALPHA_STEPS`。
- 地面图案疏密：蜂窝 `dx/dy/r = 84/72/26`（每帧 840 个路径点，四种图案里最贵）；
  十字点的空格率 `h < 0.45`；波纹振幅 7、波长 70。
- 区域色调 0.11 会不会太重（`palette.js` 的 `zoneTint`）。

### 如果不想碰表现层，三条各自独立的入口

- **玩法层**：`协同标签系统` 是投入产出比最高的一条（见第四节玩法层第一条）。
- **工程层**：`world-render.js` 已经 883 行，背景层 / 实体层 / 特效层是天然的三刀。
- **顺手的小事**：`shapes.js` 宝箱呼吸用的是 `Date.now()`，和其他动画不同步、暂停时仍在呼吸，
  应该改成吃世界时间（表现层用 `Date.now()` 不违反架构约束，所以 arch 不会拦，但节拍确实不统一）。

### 工作方式（这套流程是有效的，别退回去）

实现 → 用脚本量 → 修真问题 → **把教训固化成测试 / 断言 / 架构规则**。
第三节那一长串坑几乎每一条都对应一条现在还在跑的断言。新加东西时先问一句：
"这东西坏掉的时候，会有人报错吗？"如果答案是"不会，只是看起来不对"，就补一条断言。

## 三、已做的事

### 玩法内容
- **武器** 7 把基础（追踪弹/光环/穿透枪/地雷/回旋镖/闪电链/光束）+ 7 把进化
  （电场/爆破枪/归巢弹/链式激光/环形雷场/贯日炮/新星）+ 3 把觉醒（磁暴场/蜂群/雷暴）。
  两把材料满 3 级出进化卡，合成后腾出一个槽位；进化武器满级 + 打过 3 只交界 Boss 后，战利品里出觉醒卡。
- **词条与诅咒**：9 条通用词条（含暴击、击杀回血、拾取爆炸等改行为的），3 张诅咒卡（有代价的强化）。
- **主动技能** 4 个（震荡波/时缓/磁吸/诱饵），最多带 2 个，`Q`/`E` 手动释放。
- **兵种** 8 种，靠形状区分（圆/三角/方/五边/八边/菱/六边），对色盲友好。
- **地图元素**：岩块（挡路）、泥地（双方减速）、宝箱（走上去开，给一张免费卡）。
- **Boss**：状态机（追人 → 预警 → 出招 → 硬直），半血狂暴，预警期打够伤害可**打断**。四种原型：蛮兽（偏冲撞）、裂变者（死后裂成两只）、守卫者（护卫在场时减伤 55%）、幻影（冲撞前闪到背后）。
- **区域**：清场 45 秒 → 本区域的 Boss 堵门 → 打倒它才进下一段（荒野 → 沼泽 → 巢穴 → 雪原 → 循环），
  各自的兵种权重、地形风格、冲锋潮兵种、Boss 原型、地面图案和画面色调。Boss 战期间区域时间冻结。
- **波次节奏**：22 秒常规 → 4 秒冲锋潮（四面围一圈）→ 4 秒喘息，循环。

### 局外与目标
- **角色** 4 个：新兵（基准）/游侠/术士/猎手，各自决定起手武器和一条属性修正。
- **残片**：按存活时长和击杀结算，用来解锁角色（游侠 35 / 术士 50 / 猎手 65）和买永久强化（体质/锐度/迅捷，各 3 级）。
  中位 54 片/局，全解锁 490 片，约 10 局。打通一段区域（= 打倒一只交界 Boss）额外 +10 片。
- **通关**：打倒四个区域的交界 Boss（满级强化下通关时刻中位 270 秒），弹结算面板，可继续无尽模式；奖励 60 残片。
- **难度**：首次通关解锁噩梦（敌人血 ×1.45、移速 ×1.12、残片 ×1.5）。

### 工程与工具
- **固定步长** 1/60，`dt` 双向夹紧，最多补 5 步。同一 seed 在 30/60/144fps 下结果完全一致（断言守着）。
- **对象池** + 热循环零分配；逻辑单帧最差 1.3ms（600 怪 + 378 弹），占 16.7ms 预算的 8%。
- **批量绘制**：同色实体攒一条路径，一次 fill + 一次 stroke。550 怪场面单帧 canvas 调用 9700 → 3800，stroke 883 → 57。
- **两种合成模式**：实体走 `source-over`（要靠深色描边互相分开），光相关的东西（投射物拖尾、粒子、闪电、光环、光晕）走 `'lighter'` 叠加块。重叠处变亮而不是互相盖住。`additive(fn)` 是唯一入口，进出成对，有断言守着配平。
- **光晕精灵**：一种颜色一张 64×64 离屏图（整张填色 + `destination-in` 剪一层径向 alpha），之后每帧只 `drawImage`。不用 `shadowBlur`（每帧重算模糊）。
- **字体收口**：`view/font.js` 四个函数（`ui/bold/num/numBold`），canvas 不继承 CSS 字体栈，之前 124 处 `ctx.font` 写的是裸 `sans-serif`。
- **背景三层**：远景斑点（0.45 倍视差，固定 seed 生成一次）→ 区域各自的地面图案（方格 / 波纹 / 蜂窝 / 十字点，一次 stroke）→ 地形。区域色调从 0.07 提到 0.11。图案名的登记表是 `shapes.js` 的 `GROUND_PATTERNS`，由 `validate.js` 核对。
- **热循环用索引 for + AABB 粗筛**：600 怪 + 400 弹的极端场面单帧 1.87ms → 0.87ms，
  每帧分配 1301 字节 → 222 字节。`for...of` 每次进入都要分配迭代器，
  每只怪每帧一个 `{x, y}` 临时对象也是同一类问题。
- **快照与回放**：`snapshot`/`restore` 把世界存成纯 JSON；录像记每帧输入（行程编码），可重放核对。阵亡后按 `R` 回看这一局。
- **平衡工具**：`npm run balance`（给人看的报告）+ `npm run check`（11 条硬断言，CI 拦门）。
- **内容校验**：`validate.js` 检查所有内容表（武器/技能/兵种/地形/词条/角色/区域/Boss 原型/难度/强化）。
- **fx 事件登记表**：`emit()` 校验类型，测试双向检查每种事件都有渲染层消费方。

## 四、关键决策与踩过的坑

记下来是因为这些结论都不直观，重来一次容易再踩。

- **敌人血量必须超线性成长**（wave²）。线性的话玩家的复合成长（武器等级 × 词条倍率）必然反超，后期变无敌。
- **绕圈机器人量不出地形分布**：它半径只有 125px，地形走不出 1100px 的回收距离，量到的永远是第一个区域留下的岩块。测地形要用慢速漂移的赶路机器人。
- **冲锋潮会盖住区域配比**：后期一波 34 只强制兵种，量级压过普通刷怪。所以冲锋潮的兵种也交给区域配置，否则换区域最强烈的那一刻反而看不出区别。
- **选卡不选就等于时间停住**：写测试时忘了 `chooseUpgrade` 会导致世界卡在选卡界面，表现为"功能没生效"。已经踩过三次。
- **对象池 alloc 会复用刚释放的槽位**：分裂/裂变时必须先把父体数据抄下来，否则读到的是已经变成子体的自己。
- **减伤判定不能放在伤害入口**：Boss 一帧被打十几次，护卫扫描每帧算一次、伤害入口只读标记。
- **量化输入必须真的喂给世界**：录像量化到两位小数但当时用的是原值，60 秒的录像重放只跑到 47 秒。
- **台架有系统性偏差**：钉住的靶子会高估能反复穿透的武器（回旋镖），贴身武器在远距离测出 0。所以单体 + 群体两个台架都看，真实取舍看打 Boss 耗时和存活双指标。
- **卡池稀释会毁掉 build**：加技能卡后平均存活 118s → 73s，武器升级卡给 2 份、进化卡给 4 份才拉回来。
- **地形扫描曾是最大的逻辑热点**：每只敌人每帧要过两遍全部地形（`slowFactor` + `resolveBlock`），
  600 怪 × 35 块 × 2 = 4.2 万次开方，占了重负载下 75% 的帧时间。
  加 AABB 粗筛 + 平方距离比较后降到不到一半。
- **"热循环零分配"要连迭代器一起算**：以前只检查有没有对象字面量，漏了 `for...of` 的迭代器；
  实测它们占了每帧 3.5KB 垃圾里的大部分。
- **`heapUsed` 采样很吵**：要 `--expose-gc` 先强制回收再取基线，否则同一份代码能量出 28B/帧 和 3.5KB/帧。
- **内容改动会悄悄拖歪局外经济**：加精英原型让基准局长 116s → 90s，残片产出跟着掉一半，
  全解锁从 15 局变成 36 局，而通关线（`ZONE_SECONDS × (区域数-1)`）是写死的秒数、纹丝不动。
  以后每次加内容都要顺手看一眼 `npm run check` 的第 9 和第 11 条。
- **比例类断言全部改用中位数**：平均值会被偶尔一局 300+ 秒的雪球抬飞，
  先后出现过"噩梦比普通还长""术士是最强角色""第 2 轮比第 1 轮容易"三种假结论。
- **机器人对自动追踪武器有偏好**：它绕圈走、按固定顺序选卡，所以角色之间的绝对数字不能直接横比（猎手 58s vs 新兵 114s 不一定说明猎手弱）。
- **整局时长是长尾分布，比例类断言要用中位数 + 大种子集**：机器人偶尔滚出一局 300 秒的雪球，
  5 个种子里有一个就能把平均值抬高 30%，"更难的配置反而更长"这种结论会随机出现。
  实测轮次的中位数是 90s → 89s → 59s → 54s：一轮 +35% 血还在噪声里，两轮才咬得动。
- **别用"父体 active 变 false"当分裂测试的结束条件**：`alloc` 会复用刚释放的槽位，
  父体那个对象很可能已经变成了它的子体。改成盯分裂事件。
- **canvas 完全不继承 CSS 的 `font-family`**：`index.html` 里写了 PingFang 字体栈，但 124 处 `ctx.font` 全是裸 `sans-serif`，
  中文实际落到各平台默认 fallback（Linux 上常是衬线体）。这类问题不报错、只是"看着不太对"，很难靠看代码发现。
- **渲染烟测是一条共享的时间线**：`render.test.mjs` 里所有用例跑在同一局游戏上，
  在中间插一条会往前推帧的用例，会把后面依赖对局进度的用例挤歪（插过一次，"打倒交界 Boss 弹战利品"直接红了）。
  另外崩溃那条用例之后主循环就一直画崩溃屏，世界层不再画——新的世界层用例必须放在它之前。
- **UI 排版测试量错了所有加粗文字**：包围盒估算用 `parseFloat(font)` 取字号，而 `'bold 24px …'` 的 `parseFloat` 是 `NaN`，
  于是一路 fallback 成 13px——标题之间的重叠一律漏检。改成用正则取 `\d+px`（改完全绿，排版本身是好的）。
- **视差层不能用"最左边那个点"量位移**：贴片会绕回、边缘的点会被裁掉，两帧里可见的根本不是同一批点，
  量出来是 10.4 而不是 45。要按贴片宽度取模、整层核对每个点能不能对回原位。

## 五、接下来能做的

### 已经点名要做的
- ~~返回主界面 + 存档/读档~~ 已完成（2026-09-09）：暂停面板 `Q` 返回主界面并自动存档，首屏 `C` 继续上一局。
  取舍见第六节：读档局不再录像、阵亡/重开清档、坏档静默丢弃。后续如果想让回放跨读档，
  需要把已录制的输入流一起存进档（体积约翻倍）。

### 玩法层

**待做，按推荐顺序**（前三条的共性是"薄逻辑、宽影响"，都建立在已有系统上，不需要新美术）：

1. **协同标签系统**：给武器与技能打标签（雷/霜/血/构筑物），同标签集满 3 件解锁一个被动
   （雷 3：命中 12% 触发连锁；血 3：吸血溢出转护盾）。逻辑很薄，但把 7+7+3 的组合空间一次抬起来，
   顺便缓解觉醒可达性的问题（现在只有"凑满级进化武器"一条路通向机制变化）。
2. **区域词缀**：进区域时随机挂一条环境词缀（浓雾：视野 -35% 掉落 +50%；磁乱：吸取 -60% 宝石 ×2；
   狂潮：刷怪 ×1.4 Boss 血 -20%），可以花元进度买的"驱散"次数换掉。复用 `ZONES` + 横幅，
   四区域的重复感立刻下降一档。地面图案做完之后，词缀也更容易做出画面差别。
3. **每日种子 + 本地排行**：固定种子跑一局，存本地最好成绩。确定性 RNG 是现成的，实现成本几乎为零，
   是把"确定性"这项已有投资变现的最直接方式。
4. **新武器方向**（补充现有 17 把）：
   - 回响：延迟 1.2 秒重放你 1.2 秒前造成伤害的 40%——奖励爆发窗口，和现有持续 DPS 流派正交
   - 共鸣桩：放置型，桩之间自动连线造成伤害——第一件真正需要站位思考的武器
   - 献祭刃：消耗生命上限换伤害，和吸血系词条形成高风险闭环
   - 引力井：不造成伤害，只聚怪。纯 utility 武器会逼人重估其他武器的价值曲线
   - 霜环（控制型）/ 荆棘（反应型）：都需要先给 DPS 台架加"控制时长 / 反击伤害"维度，否则断言 2 会误判
   新武器必须**进两条进化配方**，只进一条的话它在构筑里是死路（光束那次的结论）。
5. **敌人侧的新动词**：现在所有兵种都只会"接近你"或"射你"。缺三类——附着型（贴上持续掉血，
   要靠位移甩掉）、领域型（脚下画圈，圈内减速/减伤，逼换位而不是站桩）、
   护盾型（护盾只对某个标签的伤害脆弱，给标签系统一个反向压力）。
6. **精英的"逃跑"变体**：带着稀有掉落逃离，20 秒不杀死就带走。这是唯一能让"移速"和"射程"
   两个属性在体感上被真正需要的设计。
7. **交界 Boss 的挑战契约**：Boss 战前三选一，接受额外限制换更好战利品（血量锁 50% / Boss 多一个分身）。
   直接衔接现有 `rollLoot`，把 loot 品质做成两档就行。
8. **第二货币「印记」**：只从 Boss 与契约获得，用来解锁角色/武器/词缀池，和残片（数值成长）分开。
   现在残片同时承担数值成长和解锁，导致"想解锁就得忍受弱开局"。
9. **终局：无尽塔 vs 收手**：通关点后给"收手结算 / 进塔"二选一，塔里每层 60 秒、`loopScale` 加速、只掉残片。
   给高手一个可增长的目标，不需要新内容支撑。
10. **可选房间**（最贵的一条）：区域中段生成一道门，进去是 20 秒的小房间（纯精英 / 纯宝石 / 纯躲弹），
    出来回主线。它是唯一能让"探索"这个动词存在的设计——目前地图是纯开阔面。
    放在后面是因为它第一次要改地图生成的结构。
11. **区域顺序**：现在固定，可以改成难度递增或每局随机；再往前一步是"每段两条路选一条"。
12. **遗物/击杀图鉴**：把 `achievements` 扩成图鉴（见过的敌人、用过的武器、达成的组合），
    不改玩法但给长期玩家一个收集的坑。
13. **开局携带（Loadout）**：用印记解锁"开局带某件武器 1 级"或"带一条诅咒换 +20% 残片"，
    让每局开头不再千篇一律，也是难度自定义的天然入口。
14. **更多角色**：`HEROES` 加一项即可，校验和断言会自动带上它。

**已完成的（保留原因和数字，方便回溯）：**

- ~~无尽轮次 + 长局结算图表~~ 已完成（2026-09-09）：区域循环一整轮记一轮，敌人按 `LOOP` 乘方叠强度；
  结算的击杀柱图超过 16 根就合并时间粒度（15s → 45s …）。
- ~~区域交界 = Boss 战~~ 已完成（2026-09-10）：清场时间走完就由本区域的 Boss 堵门，
  打倒它（含裂变者裂出的子体）才换景，一局变成清场 → Boss → 换景的三拍。
  `ZONE_SECONDS` 75 → 45，因为每段多出一场 Boss 战；Boss 不再按 `bossEvery` 定时刷，
  `bossTimer` 退化成"下一只 Boss 的倒计时"（哨兵值见 `SPAWN_TIMERS` 的注释）。
  实测：一段区域中位 67s（清场 45 + Boss 战约 22），两只 Boss 的间隔和改造前的 55s 差不多；
  满级强化 10 局通关 2 局、通关时刻中位 204s（改造前的通关线是写死的 150s）。
  代价：Boss 打不动就一直卡在这一段（难度仍按时间涨），中位残片一度从 43 掉到 32 片/局（全解锁 15 局），
  已用「打通一段 +10 片」补回（见下条）。
- ~~残片经济回校~~ 已完成（2026-09-10）：`earnShards` 加上 `zoneIndex × ZONE_CLEAR_BONUS`（10 片/段），
  中位回到 42 片/局、全解锁约 12 局。奖励挂在"打倒交界 Boss"而不是回调 t/kills 系数，
  是因为多出来的收入正好对应新增的那件事；段奖励算在难度倍率之前，所以噩梦局打通同样段数拿得更多。
  结算面板会把「推进 N 段 +X」和「通关 +60」分开写，否则"这局为什么多这么多"只能靠猜。
- ~~交界 Boss 的战利品~~ 已完成（2026-09-10）：打倒挡门的 Boss 后三选一（`LOOT` 表在 `upgrades.js`），
  挑完才换区。关键设计：**和升级卡共用 `chooseUpgrade` 这一个入口**——测试和平衡工具里到处是
  "暂停了就 `chooseUpgrade(i)`"，另开一个入口的话那些循环会静默卡死在暂停态上（改造过程中真踩到了：
  `rollUntilChoices`、`tools/replay.mjs` 的机器人、`test/replay.test.mjs` 的 `run` 都卡在了战利品面板上）。
  战利品只碰已有字段（player / stats / weapons / 次数），不引入新的成长维度；没有重抽和排除。
  第 9 条断言顺手从平均值改成中位数：加了战利品之后它一度报出"满级强化 0.98 倍"，纯粹是 400 秒长局把均值抬飞。
  快照加 `lootKeys`，`REPLAY_VERSION` 10。实测中位存活 83s、满级强化 108s（1.30 倍）、通关中位 169s。
- ~~屏幕外目标指示~~ 已完成（2026-09-10）：Boss / 精英出视野时在边缘画三角（`shapes.js` 的 `edgeMarker`，越远越淡），
  Boss 血条旁写「打倒它才能进 X」，出场横幅改成「巢穴的守卫者挡住了去路」。
  动机：交界 Boss 是换区的唯一条件，守卫者又偏爱一边召唤一边后缩——看不见它的时候玩家会以为倒计时卡住了。
  箭头画在暗角之后（画在之前会被压暗），几何用单元测试钉住"三角必须落在视口内且贴对应那条边"。
- ~~二段进化（觉醒）+ 新武器光束~~ 已完成（2026-09-10）：
  - **光束**（第 7 把基础武器）：锁定最近目标持续照射，热度 1.0 → 最高 2.2 倍，脱靶迅速降温。
    它是唯一的"持续单体"武器。动机来自平衡报告：交界 Boss 改成挡门之后，
    穿透枪流派 5 局里 0 局打死第一只 Boss，缺的就是能对单体持续加压的东西。
    第一版 DPS 调到 46（追踪弹 36），机器人局长从 91s 飙到 276s，压回 35 才正常。
  - **两条新配方**：光束 + 穿透枪 → 贯日炮（烧穿一条线）、光束 + 光环 → 新星（自身脉冲）。
    新武器必须进两条配方，只进一条的话它在构筑里是死路。两把都按"进化不弱于素材双持"那条断言校准过
    （贯日炮单体 183→186、新星群体 1069→1165，第一版分别只有 88 和 520，直接违反断言）。
  - **觉醒**：`AWAKENINGS` 三条线（电场→磁暴场、归巢弹→蜂群、链式激光→雷暴）。
    条件是源进化武器**满级** + 已打倒 3 只交界 Boss（`AWAKEN_ZONES`），卡从**战利品池**出。
    刻意不进升级卡池：一局只有 9–13 次升级，凑一把满级进化武器就吃掉大半，
    而卡池从十几项涨到 25 项那次平均存活直接 118s → 73s。它同时补上了无尽轮次没有目标的问题。
    觉醒是原地换武器、占同一个槽，`maxLevel 1`（终态），给机制变形而不是数值翻倍。
  - 顺手修的：跳字合并原来只在"同为暴击"或"同为普通"之间做，同一位置的 17px 暴击数和 13px 普通数
    必然叠在一起；范围武器一多就暴露了（UI 排版测试报"一帧 3 对跳字互相压着"）。现在跨暴击合并，
    只要参与的有一次暴击整坨就按暴击显示，合并半径 30 → 36。
  - 待你试玩判断：觉醒**可达但稀有**。专注型机器人跑 3 局 900 秒只有 1 局摸到（另两局卡在
    "进化了但没满级"和"没凑出进化"）。如果试玩几局都摸不到，就把门槛从"源武器满级"降到 2 级，
    或者把 `AWAKEN_ZONES` 从 3 降到 2。
- ~~第四个区域 + 第四个 Boss 原型~~ 已完成（2026-09-10）：**雪原**（地形最稀疏、冲锋兵与射手成群，
  前三段的地形是越来越密，缺一段"跑得开但压力靠数量"的）+ **幻影**（冲撞开始那一帧闪到玩家背后，
  位置只由玩家位置和朝向决定、不掷随机数，所以回放照样能重演；新增 `bossblink` fx 事件）。
  平衡上踩到的真问题：**一轮从三段变四段，把轮次加成推后了近 70 秒**，机器人有一局直接顶到 400 秒
  上限还没死（断言 4 当场变红）。修法是把 `loopScale` 的指数从"整数轮次"改成"打通的段数 ÷ 区域数"，
  并且第一圈保持 0——历史平衡阈值和通关都发生在第一圈里，动它等于把所有阈值作废。
  这样"每段涨多少"和区域总数无关，以后再加区域不会重犯，而且同一轮内部也变成逐段加压。
  第一版雪原把肉盾/召唤者压到 0.5/0.6，结果它成了全局最轻松的一段，权重回调到 0.9/1.0。
  通关时刻中位 204s → 270s（多打一只 Boss），普通局中位 110s 基本没动。
- ~~精英变体~~ 已完成（2026-09-09）：自爆者 / 护盾者 / 裂变精英，每只随机抽。
  顺手把减伤统一成实体上的 `armor` 字段（Boss 护卫和精英开盾共用），并给子弹加了 `fuse`（定时炸弹）。

### 体验层

**待做（表现层剩下的一批，都很小）：**
- **描边宽度按半径缩放**：`drawEntity` 的 `lineW` 恒定 2px，Boss 和杂兵一样粗，Boss 因此显得扁。
  建议 `clamp(r * 0.14, 1.5, 4)`。
- **Boss 内部结构**：六边形现在是一整块实色，加一个内圈 + 随狂暴变色的核心，让它读起来像"一台机器"。
- **宝石改四尖星**：圆点在密集场面里和粒子混在一起（配合已有的光晕精灵效果更好）。
- **受击方向性挤压**：已有 10% 均匀挤压，改成沿受击方向拉长会明显更有肉感。
- **宝箱呼吸改吃世界时间**：`shapes.js` 里用的是 `Date.now()`，暂停时仍在呼吸，和其他动画不同步。
- **手感开关**：降低震屏/闪白、关掉跳字、调字号——给不喜欢强反馈的人一个出口。
- **程序化 BGM**：音效已经全是 Web Audio 现场合成，BGM 同一套思路（不加载音频文件）。
- **UI 数字滚动**：残片、经验、击杀数变化时滚动而不是跳变。

**已完成的：**
- ~~成就与统计墙~~ 已完成（2026-09-10）：主菜单单独一屏，左栏 13 行累计统计、右栏 12 个成就带进度条。
  成就不存"已解锁"状态，每次从统计现算，所以加成就不用迁移存档；老存档没有 `stats` 字段会被兜成空统计。
- ~~界面分屏 + HUD 精简~~ 已完成（2026-09-09）：主菜单 / 角色选择 / 局外强化 / 操作说明各一屏；
  HUD 只留决策信息，其余进 Tab 详情浮层；暂停分三页、结算分两页。
- ~~文字重叠排查~~ 已完成（2026-09-10）：`test/ui-layout.test.mjs` 用假 ctx 拦下所有 `fillText`，
  按字号估包围盒两两求交。14 个界面 + 一组极端内容（满装备满技能、Boss 预警、第 4 轮、五位数残片、全难度通关）全过。
  抓到两处真问题：「详情（Tab）」按钮整个框压在右下角两个技能槽上（`INFO_BTN` 挪到暂停按钮右边）；
  密集场面伤害跳字一帧最多 62 对互相压着（`popNumber` 改成 30px 内累加合并，峰值 0 对，活跃跳字 47 → 19）。
- **渲染烟测里别数"整屏某种调用的总数"**：残血圈那条用例第一版是比较两次采样的 `arc` 总数，
  单跑能过、全量跑就飘（别的实体也在画 arc）。改成只数"半径正好等于 `e.r + 5`"的那种圈。
- **渲染烟测的两个脆弱点**（这次踩到）：①它是一条长会话，任何用例往世界里塞了 Boss / 改了
  `bossTimer`、`zoneBoss`、`loot`，都必须在 finally 里还回去，否则后面测换区横幅的用例会莫名变红；
  ②"把血压到 1 等它死"这种写法会被「肉」「吸」奶回来，要在循环里每帧压血，别依赖抽到哪张卡。

- ~~动作与技能的表现层打磨~~ 已完成（2026-09-10）：
  - **冲刺残影**：冲刺期间渲染层隔帧采样一个轮廓（`fx.pushGhost`），渐隐。以前冲刺只多一圈无敌光环，看不出"闪过去了"。
  - **击杀碎裂**：死掉的敌人拆成 3–5 块**同形状**小色块飞散旋转（小怪 3 块、大个子 5 块），
    比一团圆点更贴"打碎一个色块"的美术语言。为此给 `emit()` 加了一个只给表现层用的 `kind` 字段
    （fx 不进快照，所以不影响回放确定性）。碎片池 140，实测峰值 90。
  - **命中方向性喷溅**：火花沿"玩家 → 目标"方向喷而不是四散。方向由 fx 处理函数自己算（`f.x - w.player.x`），
    逻辑层不用多传字段。
  - **技能加厚**：震荡波三层环 + 一圈方向性粒子；时缓给每只敌人套冷色光环（比闪白更能表达"变慢了"）；
    磁吸 0.5 秒内给每颗经验球拉一条尾迹。
  - 硬性约束：全部只动渲染层。录像重演逐字节一致、13 条平衡断言不变可以证明这一点。
  - 第二轮（同日）：玩家**朝向**（内环朝移动方向偏心，冲刺时偏得更多）+ **挤压拉伸**
    （沿前进方向拉长、垂直压扁，幅度按渲染层自己按帧差估的速度，逻辑层不存速度字段）+
    **Boss 预警的地面指示**（冲撞画一条长度等于实际冲刺距离的带子、弹幕画一圈放射线、召唤画几个圈，
    越接近出招越实）。
  - 第三轮（同日）：**子弹差异化**（按 `b.src` 查形状表：穿透枪/激光是拉长的胶囊、回旋镖/归巢弹是菱形、
    地雷是方块，其余圆点；速度 > 300 的再加一段沿速度方向的拖尾）+ **敌人脚下阴影**
    （所有椭圆攒一条路径一次 fill）+ **相机前瞻与阻尼**（朝移动方向探出最多 46px，
    跟随系数 0.08；玩家因此不再永远在屏幕正中，绘制位置改成按相机换算）+
    **低血量红边**（低于 40% 出现，越低越明显并呼吸；血条在左上角，混战时看不过来）+
    **经验球拾取尾迹**（进入拾取范围才画）。
  - 第四轮（同日）：**升级**弹两层金色环 + 玩家发光 0.7s（升级是一局里最正向的事，原来只有一团粒子）；
    **精英/Boss 残血**（≤35%）套一圈呼吸的暗红，普通杂兵不标（标了更乱）；
    **进化/觉醒弹体光晕**（Canvas 2D 没着色器，用放大 2.1 倍的半透明同色轮廓伪造，按颜色分组画，
    额外开销是"颜色数"次 fill 而不是子弹数）；**岩块投影 + 泥地内圈波纹**（"我踩进泥里没"不用靠减速手感反推）。
  - 第五轮（同日）：**出场涟漪**（怪在视野外圈生成，进画面缺"落地"这一下）——判断方式是渲染层
    自己用一个 `Uint8Array` 记住上一帧每个池位的 active 状态，**没有新增 fx 事件**：
    fx 池只有 64 格，冲锋潮一次刷 34 只会把 hit/kill 挤掉；**眩晕标记**（头顶两个小圈，
    震荡波的价值全在"这几秒它们不动也不咬人"，不标出来只能靠"怎么不动了"自己推断）；
    **冲锋潮四边亮**（它本来就是四面围一圈刷的，中央写一行字等看完人已经被围住了）。
  - 新增了一条断言盯着批量绘制：碎片按颜色分组 + 透明度量化 4 档、时缓光环和磁吸尾迹各攒一条路径，
    密集场面下每帧 fill/stroke 仍然 < 100（逐个画会退回几百次，那正是当年掉帧的原因）。

### 工程层

**待做：**
- **拆 `world-render.js`**（883 行，现在是最大的文件）：背景层 / 实体层 / 特效层是天然的三刀。
  分界线可以照抄前几次的经验："画的是什么"比"画在哪一步"更稳定。
- **拆 `weapons.js`**（744 行）：按基础 / 进化 / 觉醒分三张表分文件，`ALL_WEAPONS` 仍在一处汇总。
- **回放倍速与拖动**：`createPlayer` 已有 `progress`/`step()`，加 2×/4×（一帧多推几步）和跳转
  （配合定期快照）成本很低。
- **手机真机复查**：虚拟摇杆和右下角技能按钮做了，界面拆屏后每屏元素更少、按钮更大，但还没上真机。
- **输入重映射**：按键现在写死在 `game.js` 里。
- **主题层**（换皮成武侠/机甲/末世）：要把文案和外观描述从内容表里抽出去，
  HUD 改成吃 view model 而不是直接 import sim/weapons/skills。工作量不小，建议等玩法定型。
- **统一 `WAVE.burstWaveSeconds`(45) 和 `SPAWN.waveSeconds`(55)**：历史遗留的不一致。
  统一到 55 会让整体明显变简单（实测平均存活 83s → 138s），所以一直没动。

**已完成的：**
- ~~src 按层分目录 + 再拆 hud.js~~ 已完成（2026-09-10）：31 个模块从平铺的 `src/` 分成五层——
  `core/`（世界与规则）`content/`（内容表）`view/`（表现层）`app/`（装配与外围）`shared/`（跨层数据）。
  意外收获：**架构检查从此不用维护文件名清单**——层级就是目录，`arch-check.mjs` 直接按目录判。
  这一步也逼出一个之前含糊的问题：内容表 import 色板、sim import 视野尺寸，算不算"逻辑层依赖表现层"？
  结论是不算（它们是数据不是行为），所以单独立了 `shared/` 层，让规则能干净地写成
  "逻辑层不许 import view/ 和 app/"。
  `hud.js` 878 → 264 行：各种面板（暂停/选卡/战利品/结算/通关/沙盒控制台，657 行）搬进 `view/panels.js`，
  分界线是"每帧都画 vs 一帧只开一个"。`hud.js` 现在同时负责把 panels/screens 组装成一套对外 API，
  所以 `app/game.js` 那边只认识 `createHud`。
  搬迁照例漏了三处模块级常量（`TAKEN_NAME`、`ZONE_SECONDS`、`statBars`），渲染烟测三次报 `ReferenceError`——
  这已经是这类重构的固定剧本了。
- ~~架构约束进 CI~~ 已完成（2026-09-10）：`tools/arch-check.mjs`（零依赖，读文件 + 正则 + 断言）。
  守四条：①逻辑层不许 `Math.random` / `Date.now` / `performance.now`（录像和快照会静默不可重演）；
  ②逻辑层不许碰 DOM、不许 import 表现层；③`enemies/weapons/skills/terrain/upgrades/zones/bosses/elites`
  不许 import `sim.js`（循环依赖，能力靠 ctx 注入）；④颜色只能来自 `palette.js`、`emit` 的事件名必须已登记。
  写的时候踩到两点：**必须先剥注释**（注释里提 localStorage 是在解释"为什么不能用"，第一版误报了
  `meta.js` / `achievements.js`），以及 `validate.js` 不该算逻辑层（它是内容检查器，要读 `layout.js` 的常量）。
  它当场抓出一处真问题：三个区域的 `tint` 是硬编码 rgba，已挪进 `palette.js` 的 `zoneTint`。
  验证方式是故意往 `zones.js` 塞一个 `Math.random()`，确认它变红。
- ~~拆 `game.js`（第一刀）~~ 已完成（2026-09-10）：把武器沙盒整块（161 行）拆成 `src/sandbox.js`，
  通过 `host`（`getWorld` / `beginSandboxRun` / `exitToMenu`）拿宿主能力。
  `game.js` 1743 → 1622 行。剩下的两刀（`input.js` 输入层、`progress.js` 局外状态）还没动。
  搬迁时唯一的坑：模块顶层的可变状态跨文件后要么变闭包要么变显式传参——
  漏改了四处 `sandboxSpawn(world, ...)`，渲染烟测立刻报 `world is not defined`。
- ~~拆 `game.js`（第二刀）~~ 已完成（2026-09-10）：局外进度（最好成绩 / 角色 / 残片解锁 / 难度）
  整块搬进 `src/progress.js`（111 行，工厂 + getter）。`game.js` 1622 → 1573 行。
  搬迁踩到的坑：用正则批量把 `meta` / `difficulty` 改成 `progress.meta` / `progress.difficulty` 时，
  连 `from './meta.js'` 和 `from './difficulty.js'` 的**路径字符串**一起改了，模块直接找不到；
  另外 `getHero` / 方向键选角还引用着已经搬走的 `heroId`。两处都是渲染烟测立刻报出来的。
  **输入层（`input.js`）这一刀没动**：键盘和指针的事件处理里，raw input 和 UI 路由
  （菜单导航、暂停、沙盒点击、回放退出）是交织的，硬拆会把路由逻辑撕成两半，得先想清楚边界。
- ~~拆 `game.js`（第三刀：世界层绘制）~~ 已完成（2026-09-10）：`src/world-render.js`（793 行）
  接走背景 / 地形 / 实体 / 投射物 / 特效 / 相机 / 暗角与全屏覆盖，`game.js` 1573 → 811 行。
  分界线是"读不读 UI 状态"：世界层一个都不读，HUD 和面板（`uiPaused` / `winPanel` / 选卡）留在 game.js 编排。
  搬完清理无用 import 时又漏了两个（`findBossKind` / `findEliteKind` 是 Boss 描边和精英标记要用的），
  渲染烟测立刻报 `ReferenceError`——这已经是同一类错误第三次被它抓到了。
  现在的分布：game.js 811 / world-render.js 793 / hud.js 878 / weapons.js 744 / sim.js 709 / screens.js 364。
  **`input.js` 仍然没动**：raw input 和 UI 路由交织，需要先设计"当前屏 → 处理函数"的路由表。
- ~~拆 `game.js`（第四刀：输入设备层）~~ 已完成（2026-09-10）：`src/input.js`（109 行）接走
  键盘集合 / 指针 / 触摸摇杆 / 边沿触发的冲刺与技能，`game.js` 811 → 756 行（从最初的 1743 行拆完）。
  这一刀是四刀里唯一需要**设计**的：前三刀都是整块搬走，而输入和 UI 路由原本逐行交织。
  分界线定成"设备只回答玩家的手在做什么，不回答这一下点在了哪个按钮上"。
  落地时的关键改动：`pointerdown` 里原来用同一个 `pointer` 变量兼做"命中判定坐标"和
  "按住朝这儿走的指针"，拆开成局部 `at`（判定用）+ `inputDev.setPointer(at)`（移动用），
  每个分支消费掉这一下之后 `clearPointer()` 表示"这是 UI 操作，不是移动"。
- ~~存档迁移的固化快照~~ 已完成（2026-09-10）：`test/fixtures/save-v10.json`（3.3KB）是 v10 时
  真跑出来的快照，冻在仓库里。之前那条用例是"把当前快照改个版本号"，证明不了真实旧档还能读——
  旧档里可能有当时特有的字段组合。新用例除了读得进来，还要求"读进来能接着跑"
  （"能读但一动就炸"是最难查的那种坏）。
- ~~平衡报告产物化~~ 已完成（2026-09-10）：CI 里 `npm run balance | tee balance-report.txt` 并上传 artifact，
  改数值前后可以直接 diff 两次构建的报告，而不用本地重跑两遍。
- ~~CI 加确定性自检~~ 已完成（2026-09-10）：`npm run selftest` 录 3 局（不同 seed / 角色）再原地重演比对，
  约 2 秒。以前录像只在本地手跑，CI 里**没有任何人验"世界还是确定性的"**——
  而这条性质是快照、存档、平衡工具、崩溃复现的共同地基，坏掉却不会有任何报错。
- ~~拆 `hud.js`（局外屏）~~ 已完成（2026-09-10）：主菜单 / 角色屏 / 局外强化 / 成就墙 / 说明屏
  搬进 `src/screens.js`（364 行），`hud.js` 1206 → 879 行。分界线是"每帧都画 vs 一帧画一屏"：
  局内 HUD 要抠 canvas 调用数，局外屏几乎只是排版。`hud.js` 用 `...screens` 转发，game.js 调用方式没变。
  清理未使用的 import 时手滑删多了一个（`DIFFICULTIES` 在结算面板里还要用），渲染烟测立刻报出来。
- ~~存档字段级迁移~~ 已完成（2026-09-10）：`restore` 现在认"比当前老但不老于 `MIGRATABLE_FROM`(9)"的档，
  按缺失字段补默认值再继续；太老的仍然丢。测试逼出一个边界：**比当前新的版本也必须丢**
  （第一版把未来版本当成可迁移，那种档里可能有我们根本不认识的字段语义，硬读比丢更危险）。
- ~~`check` 快速档 + 体积断言~~ 已完成（2026-09-10）：`npm run check:quick` 105 秒 → 20 秒
  （3 个 seed + 跳过 Boss 原型和精英那两组重型检查）。关键取舍：通关率、难度比、强化倍率这类
  **样本量敏感的断言在快速档降级成提醒**——3 个 seed 里通关 0 次完全正常，拿它当红灯只会教人忽略红灯。
  `npm run size`：gzip 后 122KB / 上限 220KB，拦的是"数量级出错"（比如误把图片塞进仓库），不是几 KB 波动。
- ~~崩溃自动导出复现材料~~ 已完成（2026-09-10）：崩溃时把 `{ message, stack, replay }` 写进
  `localStorage` 的 `survivor.crash`，`report.replay` 存成文件后可以直接 `npm run replay -- crash.json`
  精确重演。这是"世界完全确定性"顺手换来的好处：崩溃不再只是一段没有上下文的堆栈。
  读档继续的局和沙盒局不录像，那时退回存世界快照。测试里故意把 `w.enemies` 弄成 null 触发一次崩溃，
  再断言存下来的录像能重演出同一局。
- ~~武器沙盒 / 测试模式~~ 已完成（2026-09-10）：主菜单第 8 项，或 `?sandbox=1` 直接进。
  左栏 17 把武器点着调等级（Shift / 右键降级），右栏开关（无敌 / 冻结刷怪 / 锁槽位 / 时间倍速）
  和靶子（杂兵 / 围一圈 20 只 / 精英 / Boss / 清空），左上角显示最近 3 秒实时输出。
  实现上没造新东西：`test/fixtures.mjs` 里的 `labWorld` + `putEnemy` 那套夹具搬到运行时就是它，
  刷怪走 `sim.js` 新加的 `sandboxSpawn`（复用真正的 `spawnEnemy`，所以血量和原型抽取跟实战一致）。
  几个刻意的约束：不写存档、不记成就、不结算残片、不录像，升级卡直接丢掉（等级手动调）——它是工具，不该污染进度数据。
  「围一圈 20 只」对应平衡台架的群体场景，让台架数字和画面能对上。
  顺手把主菜单从 7 项加到 8 项，按钮高度和间距压了一档（否则最后一项会撞到底部的残片那行字，UI 排版测试会红）。
- ~~主菜单改卡片网格 + 沙盒面板改底部可折叠~~ 已完成（2026-09-10，试玩反馈）：
  三个问题都来自"东西变多了"：菜单 8 个竖排按钮占掉大半屏；沙盒面板占左半屏且挡住左上角 HUD、还收不起来；
  调等级只能靠 Shift / 右键。
  改法：菜单变 2 列 × 4 行卡片（标题一行、说明小字另起一行，方向键在网格里走）；
  沙盒面板挪到屏幕底部（y 330 起），武器 3 列 × 6 行、每行带 `[-]` `[+]` 按钮，`Tab` 或点把手折叠。
  踩到的坑：沙盒的按键必须排在局内那套按键**之前**——Tab 在局内是详情浮层、数字键是选卡，
  排在后面的话沙盒的 Tab 永远收不到（折叠功能一开始就是这么失灵的，渲染烟测抓到）。
  另外两个渲染用例写死了"点 (380,200) 会命中第二个菜单按钮"，菜单一改布局就变成点中了"开始游戏"直接开局；
  已经改成用 `menuCardRect` / 数字键直达，不再依赖坐标。
- ~~「开始游戏」和「选择角色」互相矛盾~~ 已修（2026-09-10，试玩反馈）：两个入口都能开局，
  区别只有"用不用上次的角色"；而且角色屏里点一下卡片就直接开局，和"首屏只认明确的开局意图"打架。
  改成：主菜单去掉「选择角色」和「难度」（都是开局配置），点「开始游戏」进角色屏 →
  选角色 / 选难度 / 按「开始（回车）」才开局，ESC 退回。菜单从 8 项减到 6 项（网格 4 行 → 3 行）。
  卡片上顺手加了这个角色的最长存活（`meta.stats.heroBest`，成就墙那套统计里已经有了）。
  连带要改的：渲染烟测里写死的菜单数字键（成就 5→4、局外强化 4→3、沙盒 8→6），
  以及 `freshRun()` 这个共享夹具——它原来按 `Digit1` 就能开局，现在得再按一次回车。

### 待你上手判断的旋钮

这一节全是"机器量不出来、只能靠眼睛和手感判断"的东西。下次回来先过一遍这里。

**玩法数值**
- ~~通关线 200 秒太远~~ 已校准（2026-09-10）：`ZONE_SECONDS` 100 → 75，通关线降到 150 秒；
  残片公式和价格表同步调过（中位 20 片/局、全解锁 36 局 → 中位 43 片/局、全解锁约 11 局）。
  四区域改造后通关时刻中位又回到 270 秒，需要试玩确认这个长度可以接受。
- 猎手（回旋镖开局）在机器人手里最弱，但机器人不会风筝。自己打几局再决定要不要动。
- 游侠（穿透枪）机器人 5 局 0 局打死第一只交界 Boss，光束加进来之后有没有改善还没测。
- 四个区域的顺序现在是固定的，可以考虑改成难度递增或每局随机。
- 觉醒（二段进化）的可达性：机器人 3 局只摸到 1 次。等试玩反馈再决定要不要降门槛
  （"源武器满级"→ 2 级，或 `AWAKEN_ZONES` 3 → 2）。
- 雪原的兵种权重（`grunt 0.9 / rusher 1.6 / shooter 1.5 / tank 0.9 / summoner 1 / splitter 0.9`）
  是从"太轻松"回调过一次的，还没经过人手验证。
- 幻影 Boss 的闪现距离 210px。

**表现层（这批全都没被人眼看过）**
- 光晕强度：玩家柔光 `r × 3.4` / alpha 0.42（冲刺 ×1.5）、Boss 底光 `r × 2.8` / 0.5、
  精英 `r × 2.4` / 0.38、弹体 `r × 3.6` / 0.55、大经验球 `r × 3.4` / 0.5、环绕球 `r × 3.2` / 0.5。
- 叠加模式下粒子密集处会不会过曝（量化档数 `PARTICLE_ALPHA_STEPS = 8`）。
- 地面图案：蜂窝 `dx/dy/r = 84/72/26`、十字点空格率 0.45、波纹振幅 7 波长 70。
- 区域色调 0.11（`palette.js` 的 `zoneTint`）、远景斑点视差 0.45 / 64 个 / 色值 `P.dust`。
- 相机前瞻 46px / 阻尼 0.08、红边阈值 40%、挤压 10%、残血圈阈值 35%、时缓光环透明度。
- 击杀碎片数量（小怪 3 块 / 大个子 5 块）、跳字合并半径 36px。

## 六、返回主界面 + 存档/读档（已完成）

> 归档的设计笔记。这个功能 2026-09-09 就做完了，留着是因为里面几条取舍（读档局不录像、
> 阵亡清档、坏档静默丢弃）还在约束现在的代码，改存档相关的东西之前值得看一眼。

**当时的结论：适合一起做，而且应该一起做。**

现在 `started` 是唯一的首屏开关，一旦置 true 就没有回头路，暂停面板里只有「继续 / 静音」。单独加一个「返回主界面」按钮技术上很简单（`started = false` + 重置世界），但会产生一个难受的交互：**退出等于丢掉这一局**。玩到 3 分钟正要通关，想去买个永久强化，代价是这局白打 —— 那这个按钮多半没人敢按。

存档正好补上这一块：退出到主界面时自动存档，主界面显示「继续上一局」。两件事合起来才是一个完整的功能。

### 现成的基础
- `snapshot(w)` / `restore(snap)` 已经是纯 JSON，且带版本号（当前 v6），角色、永久强化、难度、区域进度、通关状态全都在里面。
- 实测 120 秒、场上 185 只怪时快照 **94.9KB**，占 localStorage 5MB 上限的 1.85%。存一个档完全够用。
- 恢复后继续跑和原世界逐字节一致，已有测试守着。

### 需要决定的几个点
1. **录像怎么办**：录像是"从第一帧开始的完整输入流"，读档后接不上。要么读档后放弃这一局的回放（`lastReplay = null`，最简单），要么把已录制的步数一起存进档（120 秒约 130KB，翻倍）。我倾向前者。
2. **死后要不要删档**：不删的话可以"死了再读档"反复刷通关。建议阵亡时清档，存档只用于"中断后继续"。
3. **存档版本不匹配怎么办**：`restore` 现在直接抛错。读档失败应该静默丢弃旧档回到首屏，而不是把首屏卡死 —— 这条要写进实现。
4. **什么时候写档**：退出到主界面时写一次最省事。也可以每 30 秒自动写一次（防崩溃/关标签页），代价是每次 95KB 的序列化，放在 `requestIdleCallback` 里做。

### 最终实现
1. 暂停面板加「Q 返回主界面（自动存档）」，也可以点按钮；退出时写档并回首屏。
2. 首屏角色卡上方一行「C 继续上一局：沼泽 2:31 · 术士 · 普通」，旁边一个「放弃」清档。
3. 读出来就消档（避免同一个档反复读）；阵亡和重开都清档。
4. 读档局不再录像（`recording = false`），所以那一局阵亡后没有回放。
5. 坏档/版本不匹配：`readSaveInfo` 里恢复一遍来验证，失败就直接删掉那个 key。

顺手修掉的两个真 bug（都是这次测试抓出来的）：
- `drawPausePanel` 里写的是 `best ? clock(best().t) : '暂无'`，而 `best` 是取值函数、永远为真，
  所以没有最好成绩记录时暂停面板会崩。
- `readSaveInfo` 在 `saveInfo` 的初始化表达式里被调用，它的 catch 分支却去赋值 `saveInfo`，
  撞上 TDZ 直接 ReferenceError。现在 catch 里只删 key。

另外把渲染烟测的种子固定成 `?seed=4242`：以前每次重开都用 `Date.now()` 造种子，
"能不能活过 20 秒"每次都不一样，依赖对局结果的用例就会时红时绿。

